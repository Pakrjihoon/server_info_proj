server:
  port: 8999

spring:
  # h2
  h2:
    console:
      enabled: true

  profiles:
    active: local

  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
#        show_sql: true
        format_sql: true

#  datasource:
#    driver-class-name: org.mariadb.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/air_test?allowMultiQueries=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8&characterSetResults=UTF-8
#    username: airi_test
#    password: airi1234

  datasource:
    initialization-mode: always
    sql-script-encoding: UTF-8
    hikari.jdbc-url: jdbc:h2:mem:testdb;MODE=MYSQL
    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb
    username: test
    password: 1234

logging:
  level:
    org.hibernate:
      SQL: DEBUG # to logger
      type.descriptor.sql.BasicBinder: TRACE
    org.springframework.jdbc.core:
      JdbcTemplate: DEBUG
      StatementCreatorUtils: TRACE
